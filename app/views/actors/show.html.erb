<h1><%= @actor.name %></h1>
Created by: <%= @actor.user.username %> on <%= @actor.date_readable(@actor.created_at) %> || Last Edited by: _______ on <%= @actor.date_readable(@actor.updated_at) %>
<hr>
<h2><%= @actor.age %> years old</h2>
<br>
<h3>Roles:</h3>
<ul><% @actor.characters.each do |character| %>
    <li><%= link_to character.name, character_path(character) %> on <%= link_to character.tv_show.name, tv_show_path(character.tv_show) %></li>
<% end %></ul>
<br>
<br>
<br>
<%= link_to "Edit Actor", edit_actor_path(@actor) %>
<br>
<br>
<%= button_to "Delete Actor", actor_path(@actor), :method => :delete %>