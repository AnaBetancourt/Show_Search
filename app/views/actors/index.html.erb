<% if @show %>
    <h1><%= @show.name %>'s Actors</h1>
<% else %>
    <h1>Actors</h1>
<% end %>
<hr>
<ul><% @actors.each do |actor| %>
    <li><%= link_to actor.name, actor_path(actor) %></li>
<% end %></ul>
<br>
<%= link_to "Create a new actor here", new_actor_path %>