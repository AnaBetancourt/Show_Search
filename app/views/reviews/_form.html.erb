<%= form_for([@show, @review]) do |f| %>
    <%= f.hidden_field :tv_show_id if @show %>

    <%= f.label :body, "Tell us what you thought!" %><br>
    <%= f.text_area :body %><br>
    
    <%= f.submit %>
<% end %>